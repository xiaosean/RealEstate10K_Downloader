name: realestate10k
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - requests
  - numpy
  - opencv
  - tqdm
  - pillow
  - pip
  - yt-dlp
  - pip:
      - imageio-ffmpeg
      - moviepy
